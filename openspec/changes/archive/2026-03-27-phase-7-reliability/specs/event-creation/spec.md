## ADDED Requirements

### Requirement: Интеграция валидации длины текста в флоу создания события

Обработчик создания события ДОЛЖЕН проверять длину текста перед вызовом LLM-парсера. Валидация применяется к текстовому вводу и к транскрипции голосового сообщения.

#### Scenario: Текст события превышает лимит
- **WHEN** пользователь отправляет текстовое сообщение длиной > 1000 символов для создания события
- **THEN** бот отвечает предупреждением о лимите, LLM-парсер не вызывается

#### Scenario: Транскрипция превышает лимит
- **WHEN** голосовое сообщение транскрибировано в текст длиной > 1000 символов
- **THEN** бот отвечает предупреждением о лимите, LLM-парсер не вызывается

### Requirement: Интеграция предупреждения о прошедшей дате в флоу создания события

После парсинга события через LLM обработчик ДОЛЖЕН проверять, не является ли дата прошедшей. Если да — показывать предупреждение перед стандартной карточкой подтверждения.

#### Scenario: Событие с прошедшей датой из текста
- **WHEN** пользователь пишет «Встреча вчера в 15:00» и LLM возвращает вчерашнюю дату
- **THEN** бот показывает предупреждение о прошедшей дате с кнопками подтверждения/отмены

#### Scenario: Событие с прошедшей датой из голоса
- **WHEN** пользователь голосом описывает событие и LLM возвращает прошедшую дату
- **THEN** бот показывает предупреждение о прошедшей дате (с транскрипцией) с кнопками подтверждения/отмены

#### Scenario: Подтверждение прошедшей даты — переход к стандартной карточке
- **WHEN** пользователь подтвердил создание события с прошедшей датой
- **THEN** отображается стандартная карточка подтверждения с `[✅ Да]` `[✏️ Изменить]` `[❌ Отмена]`

## MODIFIED Requirements

### Requirement: Перехват текстовых сообщений для создания событий

Обработчик в `bot/handlers/event.py` ДОЛЖЕН перехватывать все текстовые сообщения, не являющиеся командами (через фильтр `NotCommandFilter`), и инициировать флоу создания события. Перед отправкой в LLM ДОЛЖНА выполняться валидация длины текста (1–1000 символов).

#### Scenario: Пользователь отправляет обычный текст
- **WHEN** пользователь отправляет текстовое сообщение длиной 1–1000 символов, не начинающееся с `/`
- **THEN** запускается флоу создания события: валидация длины → парсинг текста → проверка даты → выбор пространства → карточка подтверждения

#### Scenario: Пользователь отправляет команду
- **WHEN** пользователь отправляет сообщение, начинающееся с `/`
- **THEN** сообщение НЕ обрабатывается хендлером событий, а маршрутизируется к соответствующему обработчику команды

#### Scenario: Пользователь отправляет слишком длинный текст
- **WHEN** пользователь отправляет текст длиной > 1000 символов
- **THEN** бот отвечает предупреждением о лимите, LLM-парсер не вызывается
