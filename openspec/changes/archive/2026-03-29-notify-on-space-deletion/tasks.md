## 1. Форматирование уведомления

- [x] 1.1 Добавить функцию `format_space_deleted_notification(space_name, admin_name)` в `app/bot/formatting.py`

## 2. Backend API — уведомление при удалении

- [x] 2.1 В `mini_app/backend/routes/spaces.py` в хендлере `delete_space`: перед удалением получить название пространства и список участников через `space_service.get_space_by_id()` и `space_service.get_space_members()`
- [x] 2.2 Получить `first_name` администратора из модели `User` по `user_id`
- [x] 2.3 После `delete_space()` и явного `session.commit()` отправить уведомления каждому участнику (кроме инициатора) через `bot.send_message()` с error isolation (try/except + logging)

## 3. Тестирование

- [x] 3.1 Проверить сценарий: удаление пространства с несколькими участниками — все (кроме админа) получают уведомление
- [x] 3.2 Проверить сценарий: единственный участник удаляет пространство — уведомления не отправляются
- [x] 3.3 Проверить сценарий: ошибка отправки одному участнику не блокирует уведомление остальным и не ломает API-ответ
