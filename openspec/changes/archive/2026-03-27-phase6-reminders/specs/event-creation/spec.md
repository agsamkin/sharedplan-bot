## MODIFIED Requirements

### Requirement: Подтверждение и сохранение события

При нажатии кнопки «✅ Да» событие ДОЛЖНО сохраняться в таблицу `events`, ДОЛЖНЫ создаваться записи `scheduled_reminders` для всех участников пространства, и уведомления ДОЛЖНЫ отправляться участникам пространства.

#### Scenario: Пользователь подтверждает событие
- **WHEN** пользователь нажимает кнопку `✅ Да` (callback `event_confirm`)
- **THEN** создаётся запись в `events` с полями `space_id`, `title`, `event_date`, `event_time`, `created_by`, `raw_input`, создаются записи в `scheduled_reminders` для всех участников пространства, отправляются уведомления участникам, карточка обновляется сообщением об успехе

#### Scenario: Создание напоминаний для участников
- **WHEN** событие подтверждено в пространстве с 3 участниками, каждый с дефолтными `reminder_settings` (1d=true, 2h=true, 15m=true)
- **THEN** создаётся 9 записей `scheduled_reminders` (3 участника × 3 включённых интервала), каждая с корректным `remind_at`, `reminder_type`, `user_id`, `event_id`

#### Scenario: Напоминание в прошлом не создаётся
- **WHEN** событие подтверждено с датой через 10 минут, у участника включены интервалы `1d`, `2h`, `15m`
- **THEN** создаётся только напоминание `0m` (если включено) или ни одного; напоминания `1d`, `2h`, `15m` не создаются, потому что их `remind_at` уже в прошлом

#### Scenario: Событие без времени — только напоминание 1d
- **WHEN** событие подтверждено без указания времени (`event_time IS NULL`), у участника включены все 6 интервалов
- **THEN** создаётся только одно напоминание с типом `1d` и `remind_at` = 09:00 накануне даты события (в часовом поясе `Europe/Moscow`)

#### Scenario: Напоминания создаются в той же транзакции
- **WHEN** событие подтверждено и создаются напоминания
- **THEN** записи `events` и `scheduled_reminders` создаются в рамках одной DB-сессии/транзакции

#### Scenario: Расчёт remind_at для события с временем
- **WHEN** событие подтверждено с датой `2026-04-05` и временем `19:00`, у участника включён интервал `2h`
- **THEN** создаётся напоминание с `remind_at = 2026-04-05T17:00:00` в часовом поясе `Europe/Moscow`
