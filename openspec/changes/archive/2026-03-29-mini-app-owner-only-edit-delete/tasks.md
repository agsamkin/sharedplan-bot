## 1. Добавить состояние владения в EventDetailPage

- [x] 1.1 Добавить state-переменную `isOwner` (boolean, default `false`) в компонент `EventDetailPage`
- [x] 1.2 При загрузке события из `getEvent(eventId)` сохранять значение `event.is_owner` в `isOwner`

## 2. Read-only режим для не-владельца

- [x] 2.1 Добавить атрибут `readOnly={!isOwner}` на input-поле «Название»
- [x] 2.2 Добавить атрибут `readOnly={!isOwner}` на input-поля «Дата» и «Время»
- [x] 2.3 Применить приглушённый стиль для read-only полей: убрать border (`border: 'none'`), снизить opacity (`opacity: 0.8`), убрать курсор редактирования

## 3. Условное скрытие кнопок управления

- [x] 3.1 Обернуть кнопку «Сохранить» в `{isOwner && (...)}` для скрытия у не-владельца
- [x] 3.2 Обернуть кнопку «Удалить событие» в `{isOwner && (...)}` для скрытия у не-владельца
- [x] 3.3 Обернуть блок `ConfirmInline` удаления в `{isOwner && (...)}` (защита от отображения подтверждения)

## 4. Динамический заголовок страницы

- [x] 4.1 Изменить заголовок Header: для `isOwner` — «Редактирование», для не-владельца — «Событие»
- [x] 4.2 Обновить заголовок в состояниях загрузки и ошибки на «Событие» (нейтральный по умолчанию)
