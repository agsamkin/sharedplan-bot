## 1. Расширение форматирования дат

- [x] 1.1 Добавить в `app/bot/formatting.py` расширенную функцию `format_date_relative`: «послезавтра» (delta=2), день недели в винительном падеже для delta 3-6 («в понедельник», «во вторник», «в среду», «в четверг», «в пятницу», «в субботу», «в воскресенье»), числовой формат для delta >= 7
- [x] 1.2 Добавить в `app/bot/formatting.py` функцию `format_date_with_weekday` для карточки подтверждения: формат «{день} {месяц} {год}, {день_недели}» (именительный падеж дня недели)
- [x] 1.3 Добавить таблицу дней недели в винительном падеже (для относительных дат) и именительном падеже (для карточки подтверждения)

## 2. Улучшение промпта LLM-парсера

- [x] 2.1 Обновить системный промпт в `app/prompts/event_parser.py`: добавить 5-7 few-shot примеров (простое событие, относительная дата, относительное время, без времени, разговорное выражение, неявное название, сложный случай)
- [x] 2.2 Уточнить правила разрешения неоднозначностей в промпте: «утром»→09:00, «днём»→14:00, «вечером»→19:00, «ночью»→22:00, «после обеда»→14:00, «в обед»→13:00
- [x] 2.3 Убедиться, что `build_messages` передаёт `current_time` в промпт (для разрешения «через N часов»)

## 3. Улучшение карточки подтверждения события

- [x] 3.1 Обновить `format_confirmation` в `app/bot/formatting.py`: использовать `format_date_with_weekday` для даты (добавить день недели)
- [x] 3.2 Обновить `format_past_date_warning` в `app/bot/formatting.py`: использовать `format_date_with_weekday`

## 4. Улучшение уведомлений о событиях

- [x] 4.1 Обновить `format_notification` в `app/bot/formatting.py`: использовать расширенный `format_date_relative` для дат

## 5. Улучшение сообщений напоминаний

- [x] 5.1 Обновить `format_reminder_message` в `app/services/reminder_service.py`: добавить строку с датой события (день, месяц, день недели)
- [x] 5.2 Для напоминания `1d` с временем — показать «Завтра в {time}», для `1d` без времени — «Завтра, {дата}»
- [x] 5.3 Для напоминания `0m` — показать «Сейчас ({time})»

## 6. Улучшение текстов ошибок

- [x] 6.1 Обновить `PARSE_ERROR_MESSAGES` в `app/bot/formatting.py`: сделать формулировки более дружелюбными и информативными (добавить подсказку формата в ошибку invalid_json)
- [x] 6.2 Обновить `STT_ERROR_MESSAGES` в `app/bot/formatting.py`: улучшить формулировки ошибок транскрипции (добавить предложение альтернативы во все типы ошибок)

## 7. Улучшение приветствия, справки и сообщений пространств

- [x] 7.1 Обновить приветственное сообщение в `app/bot/handlers/start.py`: сделать более информативным (возможности бота, следующий шаг)
- [x] 7.2 Обновить справку `/help` в `app/bot/handlers/help.py`: сгруппировать команды по категориям, добавить инструкцию по добавлению событий
- [x] 7.3 Обновить описания команд в `app/bot/commands.py` для единообразного стиля
- [x] 7.4 Улучшить формулировки сообщений в `app/bot/handlers/space.py`: FSM-промпт «Как назовём пространство?», подтверждение с ✅, формат списка в /spaces с эмодзи ролей
- [x] 7.5 Обновить сообщения «нет пространств» во всех хендлерах для единообразной формулировки

## 8. Расширение относительных дат в списке событий

- [x] 8.1 Обновить `app/bot/handlers/events_list.py` для использования расширенного `format_date_relative` (послезавтра, день недели)
