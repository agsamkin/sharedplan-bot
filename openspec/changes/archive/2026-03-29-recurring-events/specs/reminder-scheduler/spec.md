## ADDED Requirements

### Requirement: Фоновая генерация вхождений
Новая задача `generate_recurring_occurrences()` в `scheduler/jobs.py` ДОЛЖНА запускаться раз в сутки (через APScheduler, в том же event loop). Задача находит все родительские повторяющиеся события (`recurrence_rule IS NOT NULL AND parent_event_id IS NULL`), для каждого генерирует недостающие вхождения до `current_date + 60 дней`. Для каждого нового вхождения создаёт напоминания для всех участников пространства. Задача идемпотентна — проверяет существование вхождения по `parent_event_id + event_date` перед созданием.

#### Scenario: Генерация новых вхождений на следующий день
- **WHEN** задача запускается и для еженедельного события последнее вхождение на 2026-04-20, а текущая дата 2026-03-29
- **THEN** вхождение на 2026-04-27 создаётся (попадает в окно +60 дней), с напоминаниями

#### Scenario: Идемпотентность
- **WHEN** задача запускается повторно
- **THEN** уже существующие вхождения НЕ дублируются

#### Scenario: Ошибка одного события не блокирует остальные
- **WHEN** при генерации вхождений для одного события происходит ошибка
- **THEN** ошибка логируется, обработка остальных событий продолжается

### Requirement: Формат напоминания с повторением
Сообщения напоминаний для вхождений повторяющихся событий ДОЛЖНЫ использовать тот же формат, что и одноразовые события (без дополнительной пометки о повторении — напоминание привязано к конкретной дате вхождения).

#### Scenario: Напоминание о вхождении повторяющегося события
- **WHEN** наступает время напоминания для дочернего вхождения
- **THEN** сообщение идентично напоминанию для одноразового события (содержит title, время, дату, пространство)
