## MODIFIED Requirements

### Requirement: Удаление события владельцем

Владелец события ДОЛЖЕН иметь возможность удалить своё событие с подтверждением действия через Mini App. При отправке DELETE-запроса API-клиент НЕ ДОЛЖЕН устанавливать заголовок `Content-Type` при отсутствии тела запроса. Во время выполнения запроса удаления интерфейс ДОЛЖЕН показывать состояние загрузки и блокировать повторные нажатия. После успешного удаления Mini App ДОЛЖЕН выполнить навигацию на страницу пространства по абсолютному пути (не через `navigate(-1)`), чтобы гарантировать обновление списка событий.

#### Scenario: Запрос подтверждения удаления
- **WHEN** владелец нажимает кнопку «Удалить событие» в Mini App
- **THEN** Mini App показывает диалог «Удалить «{title}»? Напоминания тоже будут удалены.» с кнопками «Удалить» и «Отмена»

#### Scenario: Подтверждение удаления — состояние загрузки
- **WHEN** владелец подтверждает удаление в Mini App
- **THEN** кнопка «Удалить» заменяется на «Удаление...» и становится неактивной, отправляется DELETE-запрос без заголовка `Content-Type`

#### Scenario: Успешное удаление — навигация
- **WHEN** DELETE-запрос завершается успешно (HTTP 204)
- **THEN** Mini App показывает toast «Событие удалено» и выполняет навигацию на страницу пространства `/spaces/{space_id}` с `replace: true`, SpaceDetailPage монтируется заново и загружает актуальный список событий

#### Scenario: Ошибка удаления
- **WHEN** DELETE-запрос завершается ошибкой
- **THEN** Mini App показывает toast с текстом ошибки, снимает состояние загрузки, диалог подтверждения остаётся открытым

#### Scenario: Отмена удаления
- **WHEN** владелец нажимает «Отмена» в диалоге подтверждения
- **THEN** диалог закрывается, событие остаётся без изменений

#### Scenario: Удаление несуществующего события
- **WHEN** владелец подтверждает удаление события, которое уже было удалено
- **THEN** API возвращает 404, Mini App показывает toast «Событие не найдено»

#### Scenario: DELETE-запрос без Content-Type
- **WHEN** API-клиент отправляет запрос методом DELETE без тела
- **THEN** заголовок `Content-Type: application/json` НЕ ДОЛЖЕН присутствовать в запросе
