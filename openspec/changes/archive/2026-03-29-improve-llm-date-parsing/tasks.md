## 1. Рефакторинг структуры промпта

- [x] 1.1 Разбить `SYSTEM_PROMPT` в `app/prompts/event_parser.py` на `PROMPT_HEADER` (роль, формат, правила) и `PROMPT_FOOTER` (финальное напоминание с якорной датой "завтра")
- [x] 1.2 Создать функцию `build_system_prompt(current_date, current_time)`, собирающую промпт из header + динамические примеры + footer
- [x] 1.3 Добавить в header явную инструкцию о приоритете текущей даты: "Все относительные даты вычисляй ТОЛЬКО от указанной выше даты «Сегодня»"
- [x] 1.4 Добавить в footer напоминание: "Помни: «завтра» = {tomorrow_date}"

## 2. Динамическая генерация примеров

- [x] 2.1 Создать хелпер-функцию `next_weekday(current_date, target_weekday) -> date` для вычисления ближайшего дня недели
- [x] 2.2 Определить примеры как декларативные структуры с относительными смещениями вместо хардкоженных дат
- [x] 2.3 Реализовать функцию `build_examples(current_date, current_time) -> str`, генерирующую строку примеров с вычисленными датами
- [x] 2.4 Добавить новые примеры: "сегодня вечером", "через 2 часа", граничный случай конца месяца

## 3. Интеграция в существующий код

- [x] 3.1 Обновить `build_messages()` — использовать `build_system_prompt()` вместо `SYSTEM_PROMPT.format()`
- [x] 3.2 Обновить `build_messages_reinforced()` аналогично, добавляя `REINFORCED_SUFFIX`
- [x] 3.3 Удалить старую константу `SYSTEM_PROMPT` после полной замены

## 4. Тестирование

- [x] 4.1 Написать юнит-тесты для `next_weekday()` — все дни недели, граничные случаи
- [x] 4.2 Написать юнит-тесты для `build_examples()` — проверить что даты в примерах консистентны с current_date
- [x] 4.3 Написать юнит-тест для `build_system_prompt()` — проверить наличие инструкции приоритета и якорной даты "завтра"
- [x] 4.4 Проверить регрессию: существующие тесты парсера проходят без изменений
