## MODIFIED Requirements

### Requirement: Системный промпт с контекстом даты

Модуль `prompts/event_parser.py` ДОЛЖЕН предоставлять функцию `build_messages(user_text, current_date, weekday, current_time)`, формирующую список сообщений для LLM API.

Системный промпт ДОЛЖЕН включать: текущую дату, день недели, текущее время, часовой пояс `Europe/Moscow`, описание ожидаемого JSON-формата `{"title": "...", "date": "YYYY-MM-DD", "time": "HH:MM" | null}`, инструкцию возвращать `time: null` если время не указано.

Промпт ДОЛЖЕН содержать явную инструкцию о приоритете текущей даты:
> "Все относительные даты вычисляй ТОЛЬКО от указанной выше даты «Сегодня»."

Промпт ДОЛЖЕН завершаться напоминанием с конкретной датой "завтра" для якорения:
> "Помни: «завтра» = {tomorrow_date}, текущая дата для всех вычислений — {current_date}."

Few-shot примеры ДОЛЖНЫ генерироваться динамически относительно `current_date`. Все даты в примерах ДОЛЖНЫ быть консистентны с переданной `current_date`. Статические (хардкоженные) даты в примерах НЕ ДОПУСКАЮТСЯ.

Промпт ДОЛЖЕН содержать few-shot примеры, покрывающие:
- Простое событие с датой и временем ("завтра в 19:00")
- Относительная дата с днём недели ("в понедельник в 8 утра")
- Событие без времени (день рождения, праздник)
- Разговорные выражения времени ("после обеда", "утром", "вечером")
- Неявное название (действие вместо события)
- Сложное относительное выражение ("на следующей неделе в среду вечером")
- Время через дефис ("в 14-15" = 14:15)
- Явный временной диапазон ("с 14 до 15", "08:00 - 09:30")
- Событие "сегодня" с разговорным временем ("сегодня вечером")
- Относительное время ("через 2 часа")
- Абсолютная дата в будущем (Новый год)

Функция `build_examples(current_date: date, current_time: time | None) -> str` ДОЛЖНА возвращать строку с отформатированными few-shot примерами, где все даты вычислены от `current_date`.

Функция `build_system_prompt(current_date: date, current_time: time | None) -> str` ДОЛЖНА собирать финальный системный промпт из заголовка (роль, дата, правила), динамических примеров и финального напоминания.

Промпт ДОЛЖЕН содержать явные правила разрешения неоднозначностей:
- «утром» → 09:00, «днём» → 14:00, «вечером» → 19:00, «ночью» → 22:00
- «после обеда» → 14:00, «в обед» → 13:00
- «в 8 утра» → 08:00, «в 9 вечера» → 21:00
- «в понедельник» → ближайший понедельник (если сегодня понедельник — следующий)
- «на следующей неделе в среду» → среда следующей недели
- Конструкция «в ЧЧ-ММ» (где ММ < 60) ДОЛЖНА интерпретироваться как время с минутами (14-15 → 14:15, 9-30 → 09:30), а НЕ как временной диапазон
- Временной интервал ДОЛЖЕН определяться ТОЛЬКО при явном формате «HH:MM - HH:MM» (с двоеточием) или конструкции «с X до Y» / «от X до Y»

#### Scenario: Формирование сообщений с динамическими примерами
- **WHEN** вызван `build_messages("Встреча завтра", date(2026, 3, 29), "воскресенье", time(14, 30))`
- **THEN** возвращается список сообщений, в котором системный промпт содержит "Сегодня: 2026-03-29 (воскресенье)" и все даты в few-shot примерах вычислены от 2026-03-29

#### Scenario: Примеры консистентны с текущей датой
- **WHEN** `current_date = date(2026, 3, 29)` (воскресенье)
- **THEN** пример "завтра в 19:00" содержит дату `2026-03-30`, пример "в понедельник" содержит дату `2026-03-30`, промпт НЕ содержит хардкоженных дат из другого дня

#### Scenario: Финальное напоминание содержит дату "завтра"
- **WHEN** `current_date = date(2026, 3, 29)`
- **THEN** промпт заканчивается строкой с "«завтра» = 2026-03-30"

#### Scenario: Парсинг "завтра" возвращает корректную дату
- **WHEN** вызван `parse_event("Завтра обед в 14")` при текущей дате 2026-03-29
- **THEN** возвращается `ParsedEvent` с `event_date=2026-03-30` (не 2026-03-28 и не 2026-03-29)

#### Scenario: Парсинг "сегодня вечером"
- **WHEN** вызван `parse_event("Сегодня вечером ужин")` при текущей дате 2026-03-29
- **THEN** возвращается `ParsedEvent` с `event_date=2026-03-29`, `event_time=19:00`

#### Scenario: Парсинг разговорного выражения «после обеда»
- **WHEN** вызван `parse_event("Стоматолог послезавтра после обеда")` при дате 2026-03-29
- **THEN** возвращается `ParsedEvent(title="Стоматолог", event_date=2026-03-31, event_time=14:00)`

#### Scenario: Парсинг сложного относительного выражения
- **WHEN** вызван `parse_event("Созвон с Петей на следующей неделе в среду после обеда")` при дате 2026-03-29 (воскресенье)
- **THEN** возвращается `ParsedEvent(title="Созвон с Петей", event_date=2026-04-01, event_time=14:00)`

#### Scenario: Парсинг события без явного названия
- **WHEN** вызван `parse_event("Сходить к врачу завтра утром")`
- **THEN** возвращается `ParsedEvent` с осмысленным `title` (например, "Визит к врачу"), `event_time=09:00`

#### Scenario: Время через дефис — интерпретация как время с минутами
- **WHEN** вызван `parse_event("Завтра обед в 14-15 с Леной")` при текущей дате 2026-03-29
- **THEN** возвращается `ParsedEvent(title="Обед с Леной", event_date=2026-03-30, event_time=14:15)`

#### Scenario: Явный временной интервал с двоеточием
- **WHEN** вызван `parse_event("15.04 08:00 - 09:30 Анализ мочи и крови")`
- **THEN** возвращается `ParsedEvent(title="08:00–09:30 Анализ мочи и крови", event_date=2026-04-15, event_time=08:00)`

#### Scenario: Явный диапазон через "с X до Y"
- **WHEN** вызван `parse_event("Встреча с 14 до 15 завтра")` при текущей дате 2026-03-29
- **THEN** возвращается `ParsedEvent(title="14:00–15:00 Встреча", event_date=2026-03-30, event_time=14:00)`
