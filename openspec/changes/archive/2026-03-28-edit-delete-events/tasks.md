## 1. Сервисный слой

- [x] 1.1 Добавить `event_service.get_event_for_owner(session, event_id, user_id)` — возвращает событие, только если `created_by == user_id`
- [x] 1.2 Добавить `event_service.delete_event(session, event_id)` — удаление события (каскадно удаляет напоминания)
- [x] 1.3 Добавить `event_service.update_event(session, event_id, **fields)` — обновление полей события (title, event_date, event_time)
- [x] 1.4 Добавить `reminder_service.recreate_reminders_for_event(session, event, space_id)` — удалить неотправленные напоминания и создать новые

## 2. FSM-состояния

- [x] 2.1 Создать `app/bot/states/edit_event.py` с состояниями `EditEvent`: `waiting_for_value`, `waiting_for_confirm`

## 3. Клавиатуры

- [x] 3.1 Создать клавиатуру карточки управления событием (кнопки «Изменить» / «Удалить»)
- [x] 3.2 Создать клавиатуру подтверждения удаления (кнопки «Да, удалить» / «Отмена»)
- [x] 3.3 Создать клавиатуру выбора поля для редактирования (кнопки «Название» / «Дата» / «Время»)
- [x] 3.4 Создать клавиатуру при редактировании времени с кнопкой «Убрать время»

## 4. Форматирование сообщений

- [x] 4.1 Добавить шаблон карточки управления событием (название, дата, время)
- [x] 4.2 Добавить шаблоны уведомлений об изменении и удалении события для участников

## 5. Callback-хендлеры управления событиями

- [x] 5.1 Создать `app/bot/callbacks/event_manage.py` с обработчиком `event_manage:{event_id}` — показ карточки управления
- [x] 5.2 Добавить обработчики удаления: `event_delete:{event_id}`, `event_delete_yes:{event_id}`, `event_delete_no:{event_id}`
- [x] 5.3 Добавить обработчик выбора поля: `event_edit_field:{event_id}:{field}`
- [x] 5.4 Добавить FSM-хендлер ввода нового значения (текстовое сообщение в состоянии `waiting_for_value`)
- [x] 5.5 Добавить обработчик подтверждения/отмены редактирования
- [x] 5.6 Добавить обработчик «Убрать время» для установки `event_time = NULL`

## 6. Модификация списка событий

- [x] 6.1 Изменить хендлер/форматирование `/events` — добавить inline-кнопки «⚙️ {title}» для событий текущего пользователя
- [x] 6.2 Передавать `user_id` в формирование списка для определения владения

## 7. Уведомления участников

- [x] 7.1 Реализовать отправку уведомлений об изменении события всем участникам пространства (кроме владельца)
- [x] 7.2 Реализовать отправку уведомлений об удалении события всем участникам пространства (кроме владельца)

## 8. Регистрация роутеров

- [x] 8.1 Зарегистрировать новый роутер `event_manage` в `app/bot/handlers/__init__.py`
