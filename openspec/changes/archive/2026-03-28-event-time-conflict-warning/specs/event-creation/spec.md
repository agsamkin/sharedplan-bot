## MODIFIED Requirements

### Requirement: Карточка подтверждения события

После парсинга и выбора пространства бот ДОЛЖЕН отправить карточку подтверждения с данными события и тремя inline-кнопками.

При наличии транскрипции (голосовой ввод) карточка ДОЛЖНА начинаться со строки `🎤 Распознано: «{transcript}»` перед данными события.

Дата в карточке ДОЛЖНА включать день недели в формате «{день} {месяц} {год}, {день_недели}» (например, «28 марта 2026, суббота»).

Если в пространстве есть конфликтующие события (в окне +-2 часа на ту же дату), перед основной карточкой ДОЛЖНО отображаться предупреждение с перечнем конфликтующих событий. Предупреждение не блокирует создание — кнопки остаются стандартные.

#### Scenario: Событие с временем
- **WHEN** парсинг вернул событие с датой 2026-03-28 (суббота) и временем 19:00
- **THEN** бот отправляет карточку формата: `📌 Событие:\n📝 {title}\n📅 28 марта 2026, суббота\n⏰ 19:00\n\nОпубликовать?` с кнопками `[✅ Да]` `[✏️ Изменить]` `[❌ Отмена]`

#### Scenario: Событие без времени
- **WHEN** парсинг вернул событие без времени (time=None)
- **THEN** карточка отображается без строки ⏰, дата включает день недели

#### Scenario: Голосовой ввод — карточка с транскрипцией
- **WHEN** событие создано из голосового сообщения и транскрипция присутствует в FSM-данных
- **THEN** карточка начинается с `🎤 Распознано: «{transcript}»\n\n`, затем стандартный формат `📌 Событие:...`

#### Scenario: Текстовый ввод — карточка без транскрипции
- **WHEN** событие создано из текстового сообщения (транскрипция отсутствует)
- **THEN** карточка отображается в стандартном формате без строки 🎤

#### Scenario: Конфликтующие события — предупреждение в карточке
- **WHEN** парсинг вернул событие на 18:00, и в пространстве уже есть «Ужин» на 19:00 и «Звонок» на 20:00
- **THEN** карточка начинается с блока предупреждения: `⚠️ На близкое время уже есть события:\n• Ужин (19:00)\n• Звонок (20:00)\n\n`, затем стандартный формат `📌 Событие:...`, кнопки остаются `[✅ Да]` `[✏️ Изменить]` `[❌ Отмена]`

#### Scenario: Нет конфликтов — карточка без предупреждения
- **WHEN** парсинг вернул событие на 18:00, и в пространстве нет событий в окне +-2 часа
- **THEN** карточка отображается в стандартном формате без блока предупреждения

#### Scenario: Событие без времени — проверка конфликтов не выполняется
- **WHEN** парсинг вернул событие без времени (time=None)
- **THEN** проверка конфликтов не выполняется, карточка отображается без предупреждения

### Requirement: Интеграция предупреждения о прошедшей дате в флоу создания события

После парсинга события через LLM обработчик ДОЛЖЕН проверять, не является ли дата прошедшей. Если да — показывать предупреждение перед стандартной карточкой подтверждения.

Если событие имеет прошедшую дату, предупреждение о конфликтах НЕ показывается (приоритет у прошедшей даты).

#### Scenario: Событие с прошедшей датой из текста
- **WHEN** пользователь пишет «Встреча вчера в 15:00» и LLM возвращает вчерашнюю дату
- **THEN** бот показывает предупреждение о прошедшей дате с кнопками подтверждения/отмены

#### Scenario: Событие с прошедшей датой из голоса
- **WHEN** пользователь голосом описывает событие и LLM возвращает прошедшую дату
- **THEN** бот показывает предупреждение о прошедшей дате (с транскрипцией) с кнопками подтверждения/отмены

#### Scenario: Подтверждение прошедшей даты — переход к стандартной карточке
- **WHEN** пользователь подтвердил создание события с прошедшей датой
- **THEN** отображается стандартная карточка подтверждения с `[✅ Да]` `[✏️ Изменить]` `[❌ Отмена]`, проверка конфликтов выполняется и предупреждение может быть показано
