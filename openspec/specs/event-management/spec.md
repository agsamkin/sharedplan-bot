# Capability: event-management

## Purpose

Управление событиями через Telegram Mini App: редактирование полей (название, дата, время), удаление с подтверждением, уведомление участников через бота и пересоздание напоминаний.

## Requirements

### Requirement: Карточка управления событием

Владелец события ДОЛЖЕН иметь возможность управлять своим событием через Telegram Mini App. В списке `/events` бота кнопка «Управление» заменяется на ссылку в Mini App.

#### Scenario: Открытие управления событием
- **WHEN** владелец хочет управлять своим событием
- **THEN** он открывает Mini App, где доступно редактирование и удаление

#### Scenario: Кнопка управления видна только владельцу
- **WHEN** пользователь просматривает события в Mini App
- **THEN** кнопки редактирования и удаления отображаются только для событий, созданных этим пользователем

#### Scenario: Событие уже удалено
- **WHEN** пользователь пытается открыть редактирование события, которое было удалено
- **THEN** Mini App показывает сообщение «Событие не найдено» и обновляет список

### Requirement: Удаление события владельцем

Владелец события ДОЛЖЕН иметь возможность удалить событие через диалог подтверждения. DELETE-запрос НЕ ДОЛЖЕН отправлять `Content-Type` при пустом теле. Показать loading-состояние, при успехе перейти на `/spaces/{space_id}` с `replace: true`. Toast при успехе/ошибке. Уведомить участников через бот. 404 → toast "Событие не найдено". Если событие повторяющееся (`recurrence_rule != null`), текст диалога ДОЛЖЕН предупреждать: "Удалить «{title}» и все будущие повторения? Это действие нельзя отменить."

#### Scenario: Удаление повторяющегося события
- **WHEN** владелец удаляет событие с `recurrence_rule != null`
- **THEN** диалог предупреждает о удалении серии, при подтверждении удаляются родительское событие и все дочерние вхождения каскадно

#### Scenario: Удаление одноразового события
- **WHEN** владелец удаляет событие с `recurrence_rule = null`
- **THEN** стандартный диалог без упоминания серии

#### Scenario: Запрос подтверждения удаления
- **WHEN** владелец нажимает кнопку «Удалить событие» в Mini App
- **THEN** Mini App показывает диалог «Удалить «{title}»? Напоминания тоже будут удалены.» с кнопками «Удалить» и «Отмена»

#### Scenario: Подтверждение удаления — состояние загрузки
- **WHEN** владелец подтверждает удаление в Mini App
- **THEN** кнопка «Удалить» заменяется на «Удаление...» и становится неактивной, отправляется DELETE-запрос без заголовка `Content-Type`

#### Scenario: Успешное удаление — навигация
- **WHEN** DELETE-запрос завершается успешно (HTTP 204)
- **THEN** Mini App показывает toast «Событие удалено» и выполняет навигацию на страницу пространства `/spaces/{space_id}` с `replace: true`, SpaceDetailPage монтируется заново и загружает актуальный список событий

#### Scenario: Ошибка удаления
- **WHEN** DELETE-запрос завершается ошибкой
- **THEN** Mini App показывает toast с текстом ошибки, снимает состояние загрузки, диалог подтверждения остаётся открытым

#### Scenario: Отмена удаления
- **WHEN** владелец нажимает «Отмена» в диалоге подтверждения
- **THEN** диалог закрывается, событие остаётся без изменений

#### Scenario: Удаление несуществующего события
- **WHEN** владелец подтверждает удаление события, которое уже было удалено
- **THEN** API возвращает 404, Mini App показывает toast «Событие не найдено»

#### Scenario: DELETE-запрос без Content-Type
- **WHEN** API-клиент отправляет запрос методом DELETE без тела
- **THEN** заголовок `Content-Type: application/json` НЕ ДОЛЖЕН присутствовать в запросе

### Requirement: Редактирование названия события

Редактирование заголовка через форму, валидация ≤500 символов, `PUT /api/events/:id`, уведомление участников. Если событие повторяющееся — обновить заголовок у всех будущих дочерних вхождений.

#### Scenario: Редактирование заголовка повторяющегося события
- **WHEN** владелец меняет title повторяющегося события
- **THEN** title обновляется у родительского события и всех дочерних вхождений с `event_date >= today`

#### Scenario: Редактирование названия
- **WHEN** владелец изменяет название в форме редактирования Mini App
- **THEN** новое название отправляется через `PUT /api/events/:id`, участники получают уведомление

#### Scenario: Слишком длинное название
- **WHEN** владелец вводит название длиннее 500 символов
- **THEN** форма показывает ошибку валидации «Название слишком длинное (макс. 500 символов)»

### Requirement: Редактирование даты события

Владелец события ДОЛЖЕН иметь возможность изменить дату своего события через date picker в Mini App.

#### Scenario: Успешное изменение даты
- **WHEN** владелец выбирает новую дату в date picker и сохраняет
- **THEN** дата обновляется через API, неотправленные напоминания пересоздаются, участники получают уведомление

### Requirement: Редактирование времени события

Владелец события ДОЛЖЕН иметь возможность изменить или убрать время своего события через Mini App.

#### Scenario: Успешное изменение времени
- **WHEN** владелец выбирает новое время в time picker и сохраняет
- **THEN** время обновляется через API, неотправленные напоминания пересоздаются, участники получают уведомление

#### Scenario: Удаление времени (событие на весь день)
- **WHEN** владелец отмечает чекбокс «Весь день»
- **THEN** время события устанавливается в NULL через API, напоминания пересоздаются

### Requirement: Редактирование повторения события

Владелец ДОЛЖЕН иметь возможность изменить `recurrence_rule` события через RepeatPicker. При изменении правила повторения все будущие дочерние вхождения удаляются и генерируются заново. При изменении с повторяющегося на одноразовое (`none`) — удаляются все дочерние вхождения. При изменении с одноразового на повторяющееся — генерируются дочерние вхождения на 60 дней.

#### Scenario: Изменение weekly на monthly
- **WHEN** владелец меняет `recurrence_rule` с `weekly` на `monthly`
- **THEN** все будущие дочерние вхождения удаляются, генерируются новые по правилу `monthly`

#### Scenario: Отключение повторения
- **WHEN** владелец меняет `recurrence_rule` на `none`
- **THEN** все дочерние вхождения удаляются, событие становится одноразовым

#### Scenario: Включение повторения
- **WHEN** владелец добавляет `recurrence_rule` к одноразовому событию
- **THEN** генерируются дочерние вхождения на 60 дней вперёд

### Requirement: Уведомления участников при изменении/удалении

Все участники пространства (кроме владельца события) ДОЛЖНЫ получать детальное уведомление через Telegram бота при редактировании или удалении события из Mini App. Уведомление ДОЛЖНО содержать достаточно информации, чтобы участник понял суть изменения без необходимости открывать Mini App.

#### Scenario: Уведомление при редактировании одного поля

- **WHEN** владелец изменяет одно поле события через Mini App (например, дату)
- **THEN** всем участникам пространства (кроме владельца) отправляется сообщение, содержащее: название пространства, название события, изменённое поле со старым и новым значением (формат «старое → новое»), имя редактора

#### Scenario: Уведомление при редактировании нескольких полей одновременно

- **WHEN** владелец изменяет несколько полей события через Mini App (например, дату и время)
- **THEN** отправляется ОДНО сообщение со всеми изменёнными полями, каждое на отдельной строке в формате «старое → новое»

#### Scenario: Формат даты в уведомлении о редактировании

- **WHEN** владелец изменяет дату события
- **THEN** старая и новая дата отображаются в формате «день месяца, день недели» (например, «28 марта, суббота → 30 марта, понедельник»)

#### Scenario: Формат времени в уведомлении о редактировании

- **WHEN** владелец изменяет время события
- **THEN** старое и новое время отображаются в формате HH:MM (например, «19:00 → 20:30»)

#### Scenario: Уведомление при удалении события

- **WHEN** владелец удаляет событие через Mini App
- **THEN** всем участникам пространства (кроме владельца) отправляется сообщение, содержащее: название пространства, название удалённого события, дату и время удалённого события, имя удалившего

#### Scenario: Формат даты/времени в уведомлении об удалении

- **WHEN** владелец удаляет событие с датой и временем
- **THEN** уведомление содержит дату в формате «день месяца, день недели» и время в формате HH:MM на одной строке (например, «28 марта, суббота, 19:00»)

#### Scenario: Удаление события без времени

- **WHEN** владелец удаляет событие без указанного времени (на весь день)
- **THEN** уведомление содержит только дату без времени

#### Scenario: Ошибка отправки уведомления
- **WHEN** уведомление не удаётся отправить одному из участников
- **THEN** ошибка логируется, остальные уведомления продолжают отправляться

### Requirement: Пересоздание напоминаний при изменении даты/времени

При изменении date/time ДОЛЖНЫ удаляться неотправленные напоминания и создаваться новые по обновлённым данным, используя `reminder_settings` участников. Для повторяющихся событий — пересоздать напоминания для всех будущих вхождений.

#### Scenario: Пересоздание напоминаний при изменении времени серии
- **WHEN** владелец меняет `event_time` повторяющегося события
- **THEN** неотправленные напоминания удаляются у родителя и всех будущих вхождений, создаются новые по обновлённому времени

#### Scenario: Пересоздание напоминаний после изменения даты
- **WHEN** владелец изменяет дату события через Mini App
- **THEN** все напоминания с `sent=FALSE` удаляются, новые напоминания создаются по настройкам каждого участника пространства на основе новой даты

#### Scenario: Пересоздание напоминаний после изменения времени
- **WHEN** владелец изменяет время события через Mini App
- **THEN** все напоминания с `sent=FALSE` удаляются, новые напоминания создаются по настройкам каждого участника пространства на основе нового времени

#### Scenario: Напоминания не пересоздаются при изменении названия
- **WHEN** владелец изменяет только название события
- **THEN** напоминания остаются без изменений

### Requirement: Только будущие события доступны для управления

Кнопки редактирования и удаления ДОЛЖНЫ отображаться только для будущих событий в Mini App.

#### Scenario: Будущее событие
- **WHEN** событие с `event_date >= текущая дата`
- **THEN** кнопки редактирования и удаления отображаются для владельца

#### Scenario: Прошедшее событие
- **WHEN** событие с `event_date < текущая дата`
- **THEN** кнопки редактирования и удаления НЕ отображаются
